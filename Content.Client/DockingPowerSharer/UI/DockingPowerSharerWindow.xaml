<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls">

    <BoxContainer Orientation="Vertical">
        <Button Name="LVButton" Text="{Loc ui-power-sharer-lv}" />
        <Button Name="MVButton" Text="{Loc ui-power-sharer-mv}" />
        <Button Name="HVButton" Text="{Loc ui-power-sharer-hv}" />
    </BoxContainer>

</controls:FancyWindow>
